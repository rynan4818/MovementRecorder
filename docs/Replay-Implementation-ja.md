# リプレイ実装・検証報告

2026-09-13追記：コピー元アバターへのHMDオフセット適用、非表示・OFF・XYZがすべて0の場合の処理省略、ON/OFFと鑑賞位置XYZの保存を追加した。結果は [オフセットと設定保存の検証報告](Replay-Source-Avatar-Offset-ja.md) を参照。

2026-09-13追記：Camera2の公開APIへリプレイ元を任意登録し、REPLAYシーンのカメラ割り当てに対応した。Camera2.dllへのアセンブリ参照は追加していない。結果は [Camera2連携の検証報告](Replay-Camera2-Integration-ja.md) を参照。

2026-09-13追記：リプレイメニュー左側の選択色が更新されない報告に対応し、記録情報を1行へ整理した。最新の結果は [行表示の修正報告](Replay-Menu-Row-Fix-ja.md) を参照。

2026-09-13追記：タブをリプレイメニューへの入口に整理し、メニュー内での再生・読込中止、選択表示の強調、コピー元アバターの表示設定を追加した。最新の結果と操作手順は [メニュー・コピー元表示の検証報告](Replay-Menu-Source-Visibility-ja.md) を参照。以下の起動UIの記載は変更前の履歴として保持する。

2026-09-13追記：鑑賞カメラを既存カメラの複製方式へ変更し、アバターの元レイヤー保持を追加した。最新の結果は [カメラ・レイヤー修正の検証報告](Replay-Camera-Clone-Layer-Fix-ja.md) を参照。

2026-09-13追記：表情の固定に対応するBlendShape同期を追加した。最新の結果は [表情同期の実装・検証報告](Replay-BlendShape-Live-Fix-ja.md) を参照。以下は2026-09-12時点の実装・検証記録として保持する。

更新日：2026-09-12  
対象：MovementRecorder `BS1.29.1`（作業開始時 HEAD `cf6c13d`）、Beat Saber 1.29.1  
状態：設計承認後の実装と、ビルド・API照合・自動テストを実施。ユーザー環境で記録一覧の表示・選択、曲の開始、アバターと既存セイバーの再生を確認済み。今回のポーズ操作修正版のゲームへの配置とHMD検証、コミットは未実施。

同日、ユーザーの実機報告を受け、記録一覧のコンポーネント型と選択イベント引数の不一致を修正した。詳細は [記録一覧の表示・選択の修正](Replay-FileList-Fix-ja.md) を参照。

続いて、リプレイ開始時にHDT Counterの必須依存がなくなる不具合をログから特定し、HDTのInstallerを止めるフックを削除した。ユーザーの指定により、HDT／HDT Counterには介入せず、距離記録を許容する。詳細は [HDT依存解決エラーの修正](Replay-HDT-Startup-Fix-ja.md) を参照。

ゲームシーンへの遷移後に曲が開始しない報告では、セイバーのTrailRendererを理由にモデルコピー全体を中止していた。付属する未対応描画の省略と、対応設定で破棄済みのTransformを参照するエラーを修正した。経緯は [モデル準備の修正](Replay-Model-Startup-Fix-ja.md) を参照。

その後の `2026.09.12.20.59.56.log` ではモデル準備が完了し、直後に独自トレイルへの標準 `ResetTrailData()` 呼び出しで停止していた。追加指定に従いセイバーのコピー・非表示化とトレイル操作を廃止し、現在ゲームが表示する左右のセイバーを記録姿勢で動かす方式へ修正した。特定のセイバーMODへ依存しない。再試行時のFPFCによるカメラ取得失敗も修正した。詳細は [既存セイバー使用と起動の修正](Replay-Native-Sabers-Fix-ja.md) を参照。

ユーザーからアバターとセイバーの正常な再生が報告され、最新ログでも21:42:29と21:44:38に音声再開と時計の進行を確認した。残るポーズ時の操作不能について、操作パネルを停止時だけ表示し、標準メニュー用コントローラーと専用入力系をHMDの観客原点に配置する修正を行った。VR中のFOV書換警告も抑止した。詳細は [ポーズパネルと観客用ポインターの修正](Replay-Pause-UI-Fix-ja.md) を参照。以下はこの修正版の自動検証結果であり、修正後のHMD操作は未確認。

## 実装内容

| 要件 | 実装した動作 | 主な場所 |
| --- | --- | --- |
| 起動UI | MOVEMENT RECORDERタブで記録を選び、専用「リプレイ」ボタンで起動。記録のEnabledとは独立。選択だけでは起動しない | `Views/SettingTabViewController.*`、`Playback/UI/ReplayMenuService.cs`、`ReplayFile*` |
| 記録一覧 | 選択譜面に合うファイルを日時順に表示。通常保存先と選択中のカスタム譜面の保存先を探索。ファイル情報・詳細・No Fail・鑑賞位置の設定 | `Playback/UI/` |
| メタデータキャッシュ | バックグラウンド走査、属性比較による差分更新、メモリとJSONのキャッシュ。追加・更新・削除、保存完了通知、再構築、キャンセル・古い結果の破棄 | `Playback/Data/MovementFileCatalog.cs` |
| 頭の移動距離 | HDTのアセンブリを参照せず、既存LiteDBの過去の値を任意表示。譜面と終了日時±3秒で一意に対応する場合のみ採用 | `Playback/Data/HeadDistance*.cs` |
| モデル再生 | アバター／OtherのTransform・メッシュ描画・ボーン・LODをコピー。コピー対象の付属する未対応描画は省略して通知。左右Saberの階層はコピー・非表示化から除外 | `Playback/Models/` |
| 姿勢と対応設定 | 実際の記録時刻で位置補間・最短経路Slerp。world姿勢と初期localScaleを使用。ルート・個別パス・左右セイバー基準・省略・欠損時表示を設定可能 | `Playback/Data/MovementClip.cs`、`Playback/Models/ModelBinding.cs` |
| セイバー表示と判定 | 記録モデルとSaberの固定関係を確認し、SaberManager更新前に既存の左右セイバーを動かす。表示・トレイル生成は現在のゲームとMODへ任せ、スコアはゲームが計算 | `Playback/Runtime/RecordedSaberDriver.cs` |
| 第三者視点 | MainCameraを基に実HMDの姿勢を使う鑑賞カメラを用意。同じ追跡原点の標準メニュー用コントローラー・専用入力系をポーズ中だけ有効化。実際の手元にセイバーは追加しない。初期位置は後方2m。FPFCにも対応 | `Playback/Runtime/SpectatorRig.cs`、`SpectatorInput.cs` |
| 再生操作 | メニューボタンで停止して操作パネルを表示。再開で非表示。停止中に先頭、前後5秒、スライダー、鑑賞位置、終了を操作。シーク後も停止を維持 | `Playback/UI/ReplayControlsViewController.*`、`Playback/Runtime/PlaybackRuntime.cs` |
| シーン内シーク | 音声・コールバック位置・生成済みノーツ等・環境演出を再構成。シーク先をまたぐ壁・アーク等を再生成。スコア等は区間初期化 | `Playback/Runtime/PlaybackSeekController.cs`、`EnvironmentReplayState.cs`、`NativeSegmentState.cs` |
| 保存抑止 | 独自の開始・終了コールバック、SiraUtil送信抑止、再生セッション限定の既知MODの記録／保存開始抑止 | `Playback/ReplaySession.cs`、`Playback/Compatibility/ReplaySaveGuards.cs` |
| 既存記録処理 | 消失対象リストの初期化、保存完了後の一覧更新通知、非同期保存中の記録バッファ解放の延期 | `Models/RecordData.cs` |

ChroMapper用の1.5倍などの空間補正・逆補正は適用していない。ゲーム上で記録されたworld座標をそのまま使う。コピー対象の元モデルの描画を抑止し、終了時に戻す。コピー側にはAnimator・IK・揺れもの等の動作スクリプトを追加しない。既存セイバーの表示と動作スクリプトは維持する。

一時停止とシークは別の処理にした。一時停止からの再開では、セイバー履歴と未完了のカット評価の時刻を停止時間だけずらし、残りの振りの評価を維持する。音声の基準時計も補正する。シークでは未完了の評価を終了・解放し、ゲームの区間状態を初期化して、移動先直前0.4秒のセイバー履歴を同じインスタンスに構築する。

保存抑止は通常Soloの成績保存コールバックを呼ばないことを基礎とする。導入済みBeatLeader 0.9.20、ScoreSaber 3.4.2、SongPlayHistoryContinuedの開始・保存処理を実DLLで照合した。これらの互換処理に必要な型やメソッドを確認できない場合は起動を止める。セッションはゲームシーンの終了処理が終わるまで有効に保ち、次の通常プレイには持ち越さない。

HeadDistanceTravelled／HDT Counterへのフックは設けない。生成・表示・距離加算・保存はHDTに任せ、リプレイ中の距離記録も許容する。HDTの導入状態や内部APIをリプレイの起動条件にはしない。過去の距離を一覧に表示する読み取り機能は維持する。

## 検証結果

| 検証 | 結果 | 検証の範囲 |
| --- | --- | --- |
| Releaseビルド | 成功 | .NET Framework 4.7.2、指定された1.29.1のゲームと導入済み依存DLLを参照 |
| 自動テスト | 65件成功、失敗・スキップ0件 | データ処理41件、モデル処理7件、セイバー／鑑賞リグ・入力17件。製品コードを.NET 10上で実行。Unityオブジェクト等のテスト用実装を使い、描画・ボーン変形・音声・XR・フレーム進行は再現しない |
| DLL・UIのAPI照合 | 197項目成功 | Harmony対象、内部フィールドと一部の型、MainCamera／FPFC、VRPointer／VRInputModule／EventSystem、埋め込みライブラリ、BSMLのバインディング名・一覧コンポーネント型・選択引数、保存抑止先、HDT向けフックとトレイル直接操作・モデルMODへのアセンブリ依存がないこと、manifest |
| 既存ファイル一覧 | 417件、ヘッダーエラー0件 | 全ファイルのヘッダー・ファイル長を読込。全417件のフレーム本体を読む試験ではない |
| 既存距離DBとの照合 | 417件に一意な候補 | 元DB・更新ログの内容、存在、サイズ、更新日時が検証前後で一致 |
| フレーム本体と姿勢評価 | 1ファイル成功 | NEO SKY, NEO MAP!の178トラック・7,009フレームを読込。先頭・中間・末尾の534姿勢を評価 |
| manifest | 変更なし | ソースの差分なし。出力DLL内の `gameVersion` も `1.20.0` |
| パッケージ | 検証用ZIP | MovementRecorder.dll、説明書、ライセンスのみ。ゲームDLL・記録ファイル・DBを含めない |

データテストには、旧形式のnull、難易度番号の省略、破損・途中切断・過大長、時刻逆行・重複、欠損トラック・巻き戻し、Quaternionの符号、キャッシュ追加／更新／削除・破損復旧・キャンセルを含む。距離側には±3秒の境界、候補重複、正当な0m、不正値、DBなし、コミット済み更新ログ、書込競合、元DBを変更しないこと、キャッシュ利用時の再確認、夏時間の曖昧な日時を含む。

モデルテストは実際の `RenderModelClone` と `SceneModelResolver` を使う。メッシュと軌跡・パーティクル等の混在、ボーンのコピー先への付け替え、LODから省略描画だけを除くこと、描画可能なメッシュがないモデルの説明、外部LOD参照による失敗時の後片付け、元の描画状態の復元と二重破棄の防止、破棄済み探索候補の除外を確認した。Unityのネイティブ描画を検証した結果ではない。

追加テストは実際の `RecordedSaberDriver`、`SpectatorRig` とモデル処理を使う。開始・シーク・再開で標準／独自トレイルのリセットを呼ばないこと、判定履歴と未完了カット時刻の保持、元コントローラーの有効状態復元、セイバーアンカーを保持したコピー対象分離、別モデルの内側にあるセイバーを含めてコピー・非表示化しないことを確認した。カメラのない論理ヘッドからMainCameraを使用すること、HMD／FPFCの表示・UI追従、観客の手元にSaberを作らないこと、終了時のカメラ・UI参照の復元も確認した。

ポーズ操作のテストは実際の `SpectatorInput` も使用した。標準メニューの親が非表示でも操作用の階層が有効になること、左右の手がHMDと同じ原点・回転・スケール・鑑賞オフセットに従うこと、入力先の切替と復元、標準側のポインター・入力モジュールとの競合回避、繰り返し表示・非表示、部分初期化の失敗時の後片付けを確認した。VR中のFOV・アスペクト書換も検出する。実際のUnityのイベント実行順やXR描画を再現するテストではない。

ビルドではゲーム側と.NET Framework側の `System.Net.Http`／`System.Windows.Forms` の参照バージョン競合に関するMSB3277警告が残る。コンパイルエラーはないが、ビルド成功やAPI照合だけでゲーム内動作を確認したことにはならない。

ローカル検証の出力はリポジトリの `artifacts/`（Git対象外）にある。

- `build-pause-ui-fix.log`：ポーズ操作修正版のビルド結果
- `tests/pause-ui-fix.trx`：65件の自動テスト結果
- `contracts-pause-ui-fix.json`：導入済みDLL・BSML等の197項目の照合結果
- `package-pause-ui-fix-verification.json`：ZIP内容と組み込んだDLLのハッシュ確認
- `contracts-hdt-old-dll-rejection.json`：修正前DLLのHDT停止フックを検出した結果
- `audit/audit.json`：実データとキャッシュの測定結果
- `MovementRecorder-Replay-BS1.29.1-preview.zip`：実機確認用パッケージ
- `MovementRecorder-Replay-BS1.29.1-pause-ui-fix.zip`：今回のポーズ操作修正版として区別した、同じ内容のパッケージ

## キャッシュの測定

2026-09-12、同一PCの.NET 10コンソールで測定した値。Unity／HMDのフレーム時間やシークの速度を測定したものではない。

| 操作 | 時間 | ヘッダー読込／キャッシュ使用 |
| --- | ---: | --- |
| 初回の一覧作成 | 約2,113 ms | 417件／0件 |
| 変更なしで再読込 | 約49 ms | 0件／417件 |
| 1件追加後の一覧更新 | 約35 ms | 1件／417件 |
| サンプル記録の全フレーム読込 | 約105 ms | 姿勢配列約35 MB |

1件追加試験では、検証用フォルダに作ったコピーを使った。ユーザーの記録フォルダに試験ファイルを作成していない。

距離DBのメモリコピーは7,544,832 bytes。初回はコピー等約111 ms（うちロック保持約3.45 ms）、LiteDB解析約147 ms、照合約3.4 msだった。キャッシュ再利用時も元DBを確認し、コピー等約6.1 ms（ロック保持約2.86 ms）で、DBの解析と再照合は省略できた。表示例の距離は57.43227 mで、UIは57.4 mを表示する。

## 実機で残っている受入確認

以下は未実施。正式な動作確認済みリリースとする前に、HMDと実際のゲームで確認する。

- [ ] 記録のEnabledがON／OFFのどちらでも、ファイル選択→タブのリプレイボタンから開始できる。曲・難易度変更や読込中止で別ファイルを開始しない。
- [ ] 準備中は音声・譜面が進まず、モデル読み込み待ちや対応エラーでもUI操作と退出ができる。
- [ ] サンプルのCustomAvatar＋SaberFactoryで全身、ボーン、材質、スケール、左右セイバーの刃先・刃元を確認する。別階層・同名候補・Other・複数モデルは手動対応も試す。
- [ ] 実HMDで見回せ、鑑賞位置を変えてもUIポインターが一致する。記録された頭の姿勢に視点が引かれない。
- [ ] 再生中はパネル・操作用コントローラーが非表示。ポーズすると実際の左右の手元に表示され、左右切替・スライダー・再開・再ポーズ・退出を操作できる。通常メニューへ戻った後も入力できる。
- [ ] 実際の手元にセイバーを表示せず、既存の左右セイバーが記録位置で動く。標準および複数のセイバーMODでモデル・軌跡・追加描画を確認する。FPFC切替後もカメラ取得とUI操作ができる。
- [ ] 記録の動きでネイティブSaberがノーツを切り、通常のノーツ判定が動く。実コントローラーや元モデルのIKが再生を上書きしない。
- [ ] カット直後に短い／長い一時停止を挟み、再開時の音声時刻、刃の速度、カット後の振りの評価が飛ばない。
- [ ] ポーズ中の±5秒、先頭、連続ドラッグ、末尾からの巻き戻しで音声・譜面・モデルが一致する。移動先付近に余分なMissや遅延加点がない。
- [ ] 長い壁、アーク／チェーン、同時刻の複数イベント、v2／v3のライト・Tweenをまたいで前後にシークする。二重生成・欠落・未来の演出状態が残らない。
- [ ] 正常終了、途中退出、モデル不一致による中止の前後で、ゲームのスコア・統計・プレイ回数、BL／SSのローカル保存・送信、プレイ履歴、MovementRecorder記録が増えない。HDTの通常／手動計測記録は許容し、HDT Counterが通常どおり動く。HDTなし・DBだけありの場合も確認する。
- [ ] リプレイ終了後、元のRenderer・カメラ・UI参照が復元される。次の通常プレイでは記録・スコア保存・HDT計測が正常に行われる。
- [ ] HMD上で一覧と操作パネルの収まり・文字・スクロール・ボタン・スライダーを確認し、モデルコピーとシークの負荷を測定する。

初期対応はSolo／Standard／両手／通常速度、MeshRenderer・SkinnedMeshRendererを中心とした描画構成。必須拡張がある譜面は対象外として開始を止める。元の乱数、表情・マテリアルアニメーション、未記録の揺れもの、記録後に変更されたモデル・譜面設定は復元できない。1.20.0互換性は未確認。

## ビルドと確認の再実行

リポジトリのルートで実行する。Visual StudioのMSBuildと.NET Framework 4.7.2のターゲットパックを使う。スクリプトは既定ではユーザーのNuGetキャッシュから復元し、別の取得元が必要な環境では `-NuGetSource` を指定できる。

```powershell
.\scripts\Build-Replay.ps1 -GameDirectory 'C:\Program Files (x86)\Steam\steamapps\common\Beat Saber' -Package
dotnet test .\MovementRecorder.Tests\MovementRecorder.Tests.csproj
.\scripts\Test-ReplayContracts.ps1 -GameDirectory 'C:\Program Files (x86)\Steam\steamapps\common\Beat Saber'
```

ビルドスクリプトはゲームへのコピー、git操作、manifest書き換えを行わない。LiteDB 5.0.21とSystem.Buffers 4.5.1は製品DLLに埋め込む。使用方法は [Replay-ja.md](Replay-ja.md)、ライブラリの表示は [THIRD-PARTY-NOTICES.md](../THIRD-PARTY-NOTICES.md) を参照。

ゲームへ配置する場合は、ゲーム終了を確認し、既存の `Plugins/MovementRecorder.dll` を作業フォルダへバックアップしてから、検証版DLLだけを置き換える。他のMODや依存DLLは上書きしない。この配置は設計書で別途確認が必要とされているため、今回まだ実施していない。
